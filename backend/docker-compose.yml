services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lopes-sistemas-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8001:8001"
    volumes:
      - .:/app
